"use client";import {useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import {Logo} from "@/components/logo";
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:''}),[error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)return setError(d.error);router.push('/')}return <main className="mesh grid min-h-screen place-items-center p-5"><div className="card w-full max-w-md rounded-[2rem] p-8"><Logo/><h1 className="mt-8 text-3xl font-black">Create your workspace</h1><p className="mt-2 text-sm text-white/40">Keep investigations and reports in one protected place.</p><form onSubmit={submit} className="mt-7 space-y-4">{[['name','Full name','text'],['email','Email','email'],['password','Password (8+ characters)','password']].map(([k,l,t])=><label className="block text-sm" key={k}><span className="mb-2 block text-white/55">{l}</span><input className="input" type={t} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}{error&&<p className="text-sm text-coral">{error}</p>}<button className="btn btn-primary w-full">Create account</button></form><p className="mt-6 text-center text-sm text-white/40">Already registered? <Link className="font-bold text-mint" href="/login">Sign in</Link></p></div></main>}
