"use client";import {useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import {Logo} from "@/components/logo";import {ArrowRight,Eye,EyeOff} from "lucide-react";
export default function Login(){const [email,setEmail]=useState('demo@phishguard.dev'),[password,setPassword]=useState('demo1234'),[show,setShow]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();setLoading(false);if(!r.ok)return setError(d.error);router.push('/')}return <main className="mesh grid min-h-screen place-items-center p-5"><div className="card w-full max-w-md rounded-[2rem] p-7 md:p-9"><Logo/><div className="my-8"><p className="text-xs font-bold uppercase tracking-[.2em] text-mint/60">Secure access</p><h1 className="mt-2 text-3xl font-black">Welcome back</h1><p className="mt-2 text-sm text-white/40">Sign in to sync scans and manage reports.</p></div><form onSubmit={submit} className="space-y-4"><label className="block text-sm"><span className="mb-2 block text-white/55">Email</span><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-sm"><span className="mb-2 block text-white/55">Password</span><div className="relative"><input className="input pr-12" type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-4 top-4 text-white/35">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label>{error&&<p className="text-sm text-coral">{error}</p>}<button className="btn btn-primary w-full" disabled={loading}>{loading?'Signing in…':<>Enter command center<ArrowRight size={17}/></>}</button></form><p className="mt-5 text-center text-xs text-white/35">Demo: demo@phishguard.dev / demo1234</p><p className="mt-6 text-center text-sm text-white/40">New here? <Link className="font-bold text-mint" href="/register">Create an account</Link></p></div></main>}
