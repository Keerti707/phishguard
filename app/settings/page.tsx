import {Shell} from "@/components/shell";import Link from "next/link";import {Database,KeyRound,LockKeyhole,Radio} from "lucide-react";
export default function Page(){return <Shell><header className="mb-7"><p className="text-xs font-bold uppercase tracking-[.2em] text-mint/60">Configuration</p><h1 className="mt-2 text-4xl font-black">Protection settings</h1></header><div className="grid max-w-4xl gap-5 md:grid-cols-2">{[[Radio,'Detection engine','Local heuristic analysis is ready with no setup.'],[KeyRound,'Threat intelligence','Add Google Safe Browsing and VirusTotal keys in your deployment environment.'],[Database,'Data storage','Connect PostgreSQL with DATABASE_URL to enable durable accounts and scan records.'],[LockKeyhole,'Account security','Sessions use signed, HTTP-only cookies and secure production defaults.']].map(([I,t,d]:any)=><section key={t} className="card rounded-3xl p-6"><I className="text-mint"/><h2 className="mt-5 text-lg font-bold">{t}</h2><p className="mt-2 text-sm leading-6 text-white/40">{d}</p></section>)}</div><Link href="/login" className="btn btn-primary mt-6">Open sign-in screen</Link></Shell>}
