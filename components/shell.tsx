"use client";import Link from "next/link";import {usePathname} from "next/navigation";import {LayoutDashboard,ScanSearch,History,ShieldAlert,Settings,Menu,X} from "lucide-react";import {Logo} from "./logo";import {useState} from "react";
const nav=[['/','Command center',LayoutDashboard],['/scanner','Live scanner',ScanSearch],['/history','Scan history',History],['/admin','Admin console',ShieldAlert]] as const;
export function Shell({children}:{children:React.ReactNode}){const path=usePathname();const [open,setOpen]=useState(false);return <div className="min-h-screen lg:grid lg:grid-cols-[260px_1fr]"><button onClick={()=>setOpen(!open)} className="fixed right-5 top-5 z-50 rounded-xl border border-white/10 bg-panel p-3 lg:hidden">{open?<X/>:<Menu/>}</button><aside className={`${open?'flex':'hidden'} fixed inset-0 z-40 flex-col bg-ink p-6 lg:sticky lg:top-0 lg:flex lg:h-screen lg:border-r lg:border-white/5`}><Logo/><nav className="mt-14 space-y-2">{nav.map(([href,label,Icon])=><Link onClick={()=>setOpen(false)} key={href} href={href} className={`flex items-center gap-3 rounded-2xl px-4 py-3 text-sm transition ${path===href?'bg-mint text-ink font-bold':'text-white/55 hover:bg-white/5 hover:text-white'}`}><Icon size={18}/>{label}</Link>)}</nav><div className="mt-auto rounded-3xl border border-mint/10 bg-mint/[.04] p-4"><div className="mb-3 flex items-center gap-2 text-sm font-bold"><span className="h-2 w-2 rounded-full bg-mint shadow-[0_0_12px_#8fffc8]"/>Protection online</div><p className="text-xs leading-5 text-white/45">Local analysis is active. Add reputation API keys for live threat feeds.</p></div><Link href="/settings" className="mt-3 flex items-center gap-3 px-4 py-3 text-sm text-white/45"><Settings size={17}/>Settings</Link></aside><main className="mesh min-w-0 p-5 pt-20 md:p-8 lg:p-10">{children}</main></div>}
